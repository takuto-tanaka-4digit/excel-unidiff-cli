# excel-unidiff-cli
Cross-platform CLI to detect and fix Unicode normalization (NFC/NFD) Git diffs in repositories and submodules, preventing phantom Excel file   changes across macOS, Linux, and Windows CI runners.
